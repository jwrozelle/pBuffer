% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prob_density_mc.R
\name{pb_mcDensity}
\alias{pb_mcDensity}
\title{Monte Carlo approximation to pBuffer displacement density}
\usage{
pb_mcDensity(
  cellMeters = 50,
  boundaries = c(5000, 10000),
  weights = c(0.99, 0.01),
  n_draws = 2e+06,
  uniform_area = FALSE,
  seed = NULL,
  na_as_zero = FALSE
)
}
\arguments{
\item{cellMeters}{Grid cell size in meters (default 50).}

\item{boundaries}{Numeric vector of max radii (meters). Example: c(5000, 10000).}

\item{weights}{Mixture weights for `boundaries`. Must sum to 1.}

\item{n_draws}{Number of Monte Carlo draws (default 2e6).}

\item{uniform_area}{If FALSE (default), sample radius uniformly on [0, R]
(matches the density used by pb_integratedDensity). If TRUE, sample uniformly
over area (radius ~ sqrt(U)*R).}

\item{seed}{Optional integer seed for reproducibility.}

\item{na_as_zero}{If TRUE, zeros are kept as 0; otherwise zeros set to NA
(default matches existing style: FALSE -> set zeros to NA).}
}
\value{
A list with elements: weightedCircle (matrix), cellMeters, radiusMeters.
}
\description{
Builds a centered probability kernel on a regular grid via Monte Carlo sampling
of displacement offsets (dx, dy) under a radial displacement model.
}
\details{
Returns a `densityBuffer` list compatible with pBuffer join functions that
expect `list(weightedCircle, cellMeters, radiusMeters)`.
}
