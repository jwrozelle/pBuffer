% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/displace_admin.R
\name{pb_assign_admin}
\alias{pb_assign_admin}
\title{Assign administrative polygon IDs to points}
\usage{
pb_assign_admin(
  point_sf,
  poly_sf,
  point_id = "DHSID",
  poly_id = "ID_2",
  newsuffix = ""
)
}
\arguments{
\item{point_sf}{An `sf` object with POINT geometry.}

\item{poly_sf}{An `sf` object with POLYGON/MULTIPOLYGON geometry.}

\item{point_id}{Name of the unique ID column in `point_sf` (default `"DHSID"`).}

\item{poly_id}{Name of the unique ID column in `poly_sf` (default `"ID_2"`).}

\item{newsuffix}{Optional suffix to append to `poly_id` in the output column name
(default `""`). Useful for creating parallel columns like `"ID_2.disp"`.}
}
\value{
A data.frame with two columns: `point_id` and `paste0(poly_id, newsuffix)`.
  Geometry is dropped.
}
\description{
Joins point locations to a polygon layer using a within-join, returning a
lightweight data.frame of point IDs and polygon IDs (geometry removed).
}
\details{
This is a helper for DHS-style displacement protocols that require displaced
points to remain within the same administrative unit as the original point.
}
\examples{
\dontrun{
admins <- pb_assign_admin(dhs_sf, adm2_sf, point_id = "DHSID", poly_id = "ID_2")
}
}
